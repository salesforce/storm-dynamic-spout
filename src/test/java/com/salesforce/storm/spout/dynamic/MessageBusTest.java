package com.salesforce.storm.spout.dynamic;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import static org.junit.Assert.*;

/**
 * Tests the MessageBus service layer, responsible for passing
 * messages across threads between DynamicSpout and VirtualSpouts.
 */
public class MessageBusTest {
    private static final Logger logger = LoggerFactory.getLogger(MessageBusTest.class);

}